name: circular_r10
description: "Stable circular orbit at r=10M (well above ISCO)"

orbit:
  mode: circular
  r0: 10.0
  phi0: 0.0

solver:
  integrator: DOP853
  tau_max: 2000.0
  rtol: 1.0e-10
  atol: 1.0e-12
  max_step: 1.0
  n_output: 10000
  stop_at_horizon: true
