{
  "name": "yoda-jump",
  "version": "1.0.0",
  "description": "Yoda Doodle Jump — lightsaber edition",
  "scripts": {
    "sync":    "npx cap sync",
    "android": "npx cap open android",
    "build-apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core":    "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
